// Generated by the flutter-flavors skill.
// Thin per-flavor entrypoint. Flutter's `-t lib/main_<flavor>.dart` target
// flag is what actually selects the flavor at the Dart level — the
// `--flavor <flavor>` CLI flag only affects native Android/iOS build
// config, it does NOT select which Dart file runs. Both must be passed
// together and must agree on the flavor name.

import 'bootstrap.dart';

void main() => bootstrap();
